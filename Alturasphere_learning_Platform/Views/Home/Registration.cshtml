<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enroll Now</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* General Styles */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f9;
}

.header {
    text-align: center;
    padding: 20px;
    background: linear-gradient(to right, #6a11cb, #2575fc);
    color: white;
}

.header h1 {
    font-size: 2.5em;
    margin: 0;
}

.header p {
    margin: 5px 0;
    font-size: 1.2em;
}

/* Enrollment Form Styles */
.enroll-container {
    max-width: 600px;
    margin: 40px auto;
    padding: 20px;
    background: #fff;
    border-radius: 10px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    animation: fadeIn 1s ease;
}

.enroll-form {
    display: flex;
    flex-direction: column;
}

.enroll-form h2 {
    font-size: 1.8em;
    margin-bottom: 10px;
    color: #2575fc;
    text-align: center;
}

.enroll-form p {
    font-size: 1em;
    margin-bottom: 20px;
    text-align: center;
    color: #555;
}

.enroll-form label {
    margin-bottom: 5px;
    font-size: 1em;
    color: #333;
}

.enroll-form input, .enroll-form select {
    margin-bottom: 15px;
    padding: 10px;
    font-size: 1em;
    border: 1px solid #ccc;
    border-radius: 5px;
    background: #f9f9f9;
    transition: border 0.3s ease;
}

.enroll-form input:focus, .enroll-form select:focus {
    border: 1px solid #2575fc;
    outline: none;
    background: #fff;
}

.submit-btn {
    padding: 10px;
    background: linear-gradient(to right, #6a11cb, #2575fc);
    color: white;
    border: none;
    border-radius: 5px;
    font-size: 1em;
    cursor: pointer;
    transition: background 0.3s ease;
}

.submit-btn:hover {
    background: linear-gradient(to right, #2575fc, #6a11cb);
}

/* Animation */
@@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Footer */


    </style>
    <script>
        document.querySelector('.enroll-form').addEventListener('submit', (e) => {
            e.preventDefault(); // Prevent the page from refreshing
            const name = document.getElementById('name').value;
            const email = document.getElementById('email').value;
            alert(`Thank you for enrolling, ${name}! A confirmation email has been sent to ${email}.`);
            window.location.href = '/dashboard'; // Redirect to the dashboard (replace with the actual URL)
        });

    </script>
</head>
<body>
    <header class="header">
        <h1>Enroll in a Course</h1>
        <p>Register now to start your learning journey!</p>
    </header>

    <main class="enroll-container">
        <form class="enroll-form" action="@Url.Action("Success", "Registration")">
            <h2>Registration Form</h2>
            <p>Fill in your details to enroll in the course.</p>
 Full Name 
<label for="name">Full Name:</label>
<input type="text" id="name" name="name" placeholder="Enter your full name" required>
 Email 
<label for="email">Email Address:</label>
<input type="email" id="email" name="email" placeholder="Enter your email address" required>
 Phone 
<label for="phone">Phone Number:</label>
<input type="tel" id="phone" name="phone" placeholder="Enter your phone number" required>
 Course Selection 
<label for="course">Select Course:</label>
<select id="course" name="course" required>
    <option value="" disabled selected>Select a course</option>
    <option value="web-development">Web Development</option>
    <option value="data-science">Data Science</option>
    <option value="mobile-development">Mobile App Development</option>
</select>
 Submit Button 
<button type="submit" class="submit-btn">Enroll Now</button>
        </form>
    </main>



    <script src="scripts.js"></script>
</body>
</html>
@*@model Alturasphere_learning_Platform.Models.User

<h2>Registration</h2>

@if (TempData["ErrorMessage"] != null)
{
    <p style="color: red;">@TempData["ErrorMessage"]</p>
}

@using (Html.BeginForm("Enroll", "Registration", FormMethod.Post))
{
    <div>
        <label>Full Name:</label>
        @Html.TextBoxFor(m => m.FullName)
        @Html.ValidationMessageFor(m => m.FullName)
    </div>
    <div>
        <label>Email:</label>
        @Html.TextBoxFor(m => m.Email)
        @Html.ValidationMessageFor(m => m.Email)
    </div>
    <div>
        <label>Phone:</label>
        @Html.TextBoxFor(m => m.Phone)
        @Html.ValidationMessageFor(m => m.Phone)
    </div>
    <div>
        <label>Course:</label>
        @Html.TextBoxFor(m => m.Course)
        @Html.ValidationMessageFor(m => m.Course)
    </div>
    <button type="submit">Enroll Now</button>
}*@
